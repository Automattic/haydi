Launch a headed browser and run the following prompt through the Claude File Assistant, auto-approving all write proposals:

```bash
node tests/run-prompt.js $ARGUMENTS
```

If wp-env isn't running, start it first:

```bash
npm run env:start
```
